# ---- core ML / audio ----
numpy>=1.24.0
faster-whisper>=1.0.0
ctc-segmentation>=1.7.4
transformers>=4.40.0
torch>=2.1.0
librosa>=0.10.1
soundfile>=0.12.1

# ---- Japanese text processing ----
pykakasi>=2.2.1

# ---- optional: vocal separation (disabled by default) ----
# demucs>=4.0.0

# ---- optional: high-precision components ----
lightning>=2.0.0

# ---- Python standard library only below this line ----
# (configparser, difflib, pathlib, wave, threading, argparse, datetime, io — no install needed)
pretty_midi
